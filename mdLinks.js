let c = console.log;

const mdLinks = (path, options) =>{
return new Promise((resolved, rejected )=> {
   if (path ===5 && options===2) 
resolved(c("hola"));
else
rejected(error); 
    
});
}
  
module.exports = mdLinks;
